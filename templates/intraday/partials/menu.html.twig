<div class="sticky-top">
	<div class="bg-zen-intraday">
		<div class="container blog-header-top">

			<header class="blog-header py-3 ">

				<div class="row flex-nowrap justify-content-between align-items-center">

					<div class="col-4">
						<a class="blog-header-logo text-dark" href="/">
							<img src="{{ asset('images/zenbourse-logo.png') }}" alt="Logo" width="150" class="d-inline-block align-text-top">
						</a>
					</div>

					<div class="col-3 pt-1">
						<h4 class="fst-italic text-light text-center" style="margin: 0">Méthode intraday</h4>
					</div>

					<div class="col-5 d-flex align-items-center justify-content-end">

						<div class="p-3">
							<a class="link-light" href="{{ path('app_investisseur') }}" style="font-size: 13px">METHODE INVESTISSEUR</a>
						</div>

						{% if app.user %}
							<a class="btn btn-sm btn-outline-light mr-2" href="{{ path('app_logout') }}">DECONNEXION</a>
						{% else %}
							<a class="btn btn-sm btn-outline-light mr-2" href="{{ path('app_login') }}">CONNEXION</a>
						{% endif %}

						<div class="btn-group" style="margin-left: 15px;">
							<button class="btn btn-light btn-sm dropdown-toggle border-0" type="button" data-bs-toggle="dropdown" aria-expanded="false">
								<span class="fi fi-fr"></span>
							</button>
							<ul class="dropdown-menu text-dark">
								<li>
									<a class="dropdown-item" href="#">
										<span class="fi fi-gb"></span>
									</a>
								</li>
							</ul>
						</div>

					</div>

				</div>

			</header>

		</div>
	</div>


	<div class="nav-scroller mb-2 blog-nav-second">

		<div class="container border-0 blog-nav">
			<nav class="nav d-flex justify-content-between text-uppercase">
				{% for menu in get_menus(url_intraday) %}
					<a class="px-2 py-3 link-secondary {% if app.request.get('_route') == menu.url%} active-intraday {% endif %}" href="{{ path(menu.url) }}">{{ menu.title }}</a>
				{% endfor %}
			</nav>

		</div>

	</div>
</div>
